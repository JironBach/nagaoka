class CreateUsers < ActiveRecord::Migration[5.1]
  def change
    create_table :users do |t|
      t.timestamps

      t.string :users, :user_id, null: false, default: ""
      t.string :users, :name, null: false, unique: true
    end

    add_index :users, :user_id, unique: true
  end
end
