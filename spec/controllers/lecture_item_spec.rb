require 'rails_helper'

describe LectureItemController, type: :controller do
  context 'GET /index/0/subject2/0/lecture_item/0' do
=begin
    it 'showを表示' do
      get :show
      expect(response).to render_template(:show)
    end

    it '名前あり、名前がマッチせず' do
      patch :update, params:{ username: 'hoge' }
      expect(response).to have_http_status(:success)
    end
=end
  end

end
